{% block main %}
<div class="container">
<h2>Gestion des Utilisateurs</h2>
<table class="table table-striped table-hover">
<thead>
<tr>
        <th scope="col"> email </td>
        <th scope="col"> password </td>
        <th scope="col"> firstname </td>
        <th scope="col"> lastname </td>
        <th scope="col"> isAdmin </td>
    </tr>
</thead>
<tbody>
	{% for user in users %}
    <tr>
        <td>
            {{ user.email }}
        </a>
        </td>
        <td>

        </td>
        <td>{{ user.firstname }}
        </td>
        <td>
            {{ user.lastname }}
        </td>
        <td> 
        <form id="update_{{ user.id }}" action="/userlist" method="post">
                <input name="id" type="hidden" value="{{ user.id }}">
                <input name="isAdmin" type="hidden" value="{{ user.isAdmin }}">
                <input name="action" type="hidden" value="update">
            <div class="form-check form-switch">
                 <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault"{% if user.isAdmin == true %}checked {% endif %}onChange="this.form.submit()">
            </div>
        </form>
        </td>
        <td>
        <form id="delete_{{ user.id }}" action="/userlist" method="post">
        <input name="id" type="hidden" value="{{ user.id }}">
        <input name="action" type="hidden" value="delete">
            <button type="submit" class="btn btn-danger">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash" viewBox="0 0 16 16">
                    <path d="M5.5 5.5A.5.5 0 0 1 6 6v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5zm2.5 0a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5zm3 .5a.5.5 0 0 0-1 0v6a.5.5 0 0 0 1 0V6z"/>
                    <path fill-rule="evenodd" d="M14.5 3a1 1 0 0 1-1 1H13v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4h-.5a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1H6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1h3.5a1 1 0 0 1 1 1v1zM4.118 4 4 4.059V13a1 1 0 0 0 1 1h6a1 1 0 0 0 1-1V4.059L11.882 4H4.118zM2.5 3V2h11v1h-11z"/>
                </svg>
            </button>
        </form>
        </td>
    </tr>
	{% endfor %}
    </tbody>
</table>
</div>
{% endblock %}
